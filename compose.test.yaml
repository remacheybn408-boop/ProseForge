services:
  legacy-test:
    build:
      context: .
      dockerfile: docker/api.Dockerfile
    command: ["pytest", "-q", "--junitxml=/artifacts/pytest.xml"]
    volumes:
      - .:/app
      - ./artifacts:/artifacts

  api-test:
    extends:
      service: legacy-test
    build:
      context: .
      dockerfile: docker/test.Dockerfile
    command: ["pytest", "-q", "--junitxml=/artifacts/api-pytest.xml"]
    environment:
      PROSEFORGE_DATABASE_URL: postgresql+asyncpg://proseforge:proseforge@postgres:5432/proseforge
    depends_on:
      postgres:
        condition: service_healthy

  contract-test:
    extends:
      service: api-test
    command: ["pytest", "-q", "tests/contract", "--junitxml=/artifacts/contract-pytest.xml"]

  migration-test:
    extends:
      service: api-test
    command: ["pytest", "-q", "tests/migration", "tests/integration/database", "--junitxml=/artifacts/migration-pytest.xml"]
    environment:
      PROSEFORGE_DATABASE_URL: postgresql+asyncpg://proseforge:proseforge@postgres:5432/proseforge
    depends_on:
      postgres:
        condition: service_healthy

  recovery-test:
    extends:
      service: api-test
    command: ["pytest", "-q", "tests/recovery", "--junitxml=/artifacts/recovery-pytest.xml"]

  web-test:
    image: node:22-alpine
    working_dir: /app/apps/web
    volumes:
      - .:/app
      - web-node-modules:/app/apps/web/node_modules
    command: ["sh", "-lc", "corepack enable && pnpm install --frozen-lockfile && pnpm test -- --run && pnpm build"]

volumes:
  web-node-modules:
