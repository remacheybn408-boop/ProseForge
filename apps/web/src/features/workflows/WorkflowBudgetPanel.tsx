export function WorkflowBudgetPanel({ used, limit }: { used: number; limit: number }) { const ratio = limit ? Math.min(1, used / limit) : 0; return <section aria-label="Workflow budget"><strong>Budget</strong><progress value={used} max={limit} /> <span>{used.toLocaleString()} / {limit.toLocaleString()} tokens</span>{ratio >= 1 && <p>Budget blocked before the next node.</p>}</section>; }
