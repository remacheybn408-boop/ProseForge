export function ReviewConflictPanel({ conflicts, onSelect }: { conflicts: { id: string; left: string; right: string }[]; onSelect?: (id: string) => void }) { return <section aria-label="Review conflicts">{conflicts.map(conflict => <article className="conflict-stamp" key={conflict.id}><strong>Conflict</strong><p>{conflict.left}</p><p>{conflict.right}</p><button type="button" onClick={() => onSelect?.(conflict.id)}>Send to proposal</button></article>)}</section>; }
