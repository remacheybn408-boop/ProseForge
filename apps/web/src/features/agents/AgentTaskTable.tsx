export function AgentTaskTable({ tasks, onRetry }: { tasks: { id: string; role: string; status: string; attempts: number }[]; onRetry?: (id: string) => void }) { return <table aria-label="Agent tasks"><tbody>{tasks.map(task => <tr key={task.id}><th>{task.role}</th><td>{task.status}</td><td>{task.attempts} retries</td><td><button type="button" onClick={() => onRetry?.(task.id)}>Retry</button></td></tr>)}</tbody></table>; }
