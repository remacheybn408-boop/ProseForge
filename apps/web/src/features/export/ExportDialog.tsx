import { useState } from "react";
import type { ExportFormat, ExportRequest } from "./exportTypes";
export function ExportDialog({ projectId, versionIds, onExport }: { projectId: string; versionIds: string[]; onExport: (request: ExportRequest) => void }) { const [format, setFormat] = useState<ExportFormat>("md"); const [title, setTitle] = useState(""); return <form aria-label="Export dialog" onSubmit={event => { event.preventDefault(); onExport({ project_id: projectId, format, version_ids: versionIds, title: title || undefined, locale: navigator.language }); }}><label>Format<select aria-label="Export format" value={format} onChange={event => setFormat(event.target.value as ExportFormat)}>{["md", "txt", "docx", "epub"].map(item => <option key={item} value={item}>{item.toUpperCase()}</option>)}</select></label><label>Title<input aria-label="Export title" value={title} onChange={event => setTitle(event.target.value)} /></label><button type="submit">Export snapshot</button></form>; }
