import type { ModelCapability } from "./modelCapabilities";
export function ModelPicker({ models, value, onChange }: { models: ModelCapability[]; value?: string; onChange: (model: ModelCapability) => void }) { return <label className="model-picker">Model<select aria-label="Model" value={value ?? ""} onChange={event => { const selected = models.find(model => `${model.provider}/${model.model_id}` === event.target.value); if (selected) onChange(selected); }}>{models.map(model => <option key={`${model.provider}/${model.model_id}`} value={`${model.provider}/${model.model_id}`}>{model.provider} / {model.model_id}</option>)}</select></label>; }
