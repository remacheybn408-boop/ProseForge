import type { ReasoningLevel } from "./modelCapabilities";
export function ReasoningPicker({ value, supported, onChange }: { value: ReasoningLevel; supported: ReasoningLevel[]; onChange: (level: ReasoningLevel) => void }) { return <label className="reasoning-picker">Reasoning<select aria-label="Reasoning strength" value={value} onChange={event => onChange(event.target.value as ReasoningLevel)}>{(["auto", "fast", "standard", "deep", "max"] as ReasoningLevel[]).map(level => <option key={level} value={level} disabled={!supported.includes(level)}>{level}</option>)}</select></label>; }
